/**
 ******************************************************************************
 * @file    logger.h
 * @brief   Библиотека логирования для STM32 - приём кодовых логов с
 *          приоритетом, вывод в отладочный интерфейс (SWO) или пользовательскую
 *          консоль, опциональная буферизация и сохранение в энергонезависимую
 *          память.
 * @author  Mechanic
 * @date    18.09.2026
 * @version 1.1
 *
 * @copyright Copyright (c) 2026 Mechanic.
 *            Свободное некоммерческое использование и модификация. Условия
 *            распространения - см. LICENSE / README.md в составе проекта.
 *
 *          === АРХИТЕКТУРА МОДУЛЯ ===
 *
 *          1. Таблица кодов логов (LOGGER_LogTable) заполняется пользователем
 *             индивидуально под свой проект в отдельном файле logger_codes.h
 *             (см. пример и требования там) - сама библиотека не содержит ни
 *             одного кода лога. Таблица ДОЛЖНА быть отсортирована по коду по
 *             возрастанию, без повторов - это проверяется один раз в
 *             LOGGER_Init(), после чего LOGGER_Log() ищет запись двоичным
 *             поиском (быстро, без malloc, без линейного перебора).
 *
 *          2. LOGGER_Log(code, value) - главная горячая функция, вызывается
 *             потенциально очень часто, поэтому:
 *              - никаких проверок конфигурации в рантайме - всё, что можно
 *                проверить заранее, проверяется один раз в LOGGER_Init();
 *              - вывод в SWO по умолчанию не использует printf/snprintf (тяжёлая
 *                разборка форматной строки, лишний стек) - минимальный ручной
 *                форматтер, пишущий байты напрямую через ITM_SendChar();
 *              - буферизация - O(1) (без учёта двоичного поиска, который сам
 *                по себе O(log N) от размера таблицы кодов).
 *
 *          3. Без инициализации (LOGGER_Init() не вызывался) LOGGER_Log()
 *             работает в минимальном режиме "только вывод в SWO", без
 *             буферизации - можно начинать логировать сразу после старта
 *             прошивки, до того как настроена/готова энергонезависимая память.
 *
 *          4. После LOGGER_Init():
 *              - если передана console_fn - вывод идёт в неё вместо SWO;
 *              - если передана write_fn - включается буферизация: логи
 *                копятся в общем статическом буфере (LOGGER_BUFFER_CAPACITY
 *                записей, без malloc). Библиотека хранит максимальный
 *                приоритет среди уже накопленных записей и количество записей
 *                именно этого приоритета. Как только это количество достигает
 *                настроенного для данного приоритета порога - весь буфер
 *                (код+значение каждой записи, без текстового описания)
 *                одним куском передаётся в write_fn (указатель + длина в
 *                байтах), буфер очищается. Пороги задаются отдельно на каждый
 *                из 3 уровней приоритета - логи более высокого приоритета,
 *                как правило, настраиваются с меньшим порогом, чтобы попадать
 *                в память чаще.
 *              - если write_fn не передана - буферизация не включается
 *                (копить данные некуда и незачем), логи только выводятся.
 *
 *          5. Формат одной буферизованной записи - LOGGER_Record_t (код +
 *             значение, 8 байт, без выравнивающих "дыр"). Именно в таком виде
 *             (массив таких записей подряд) данные приходят в write_fn -
 *             пригодится при написании кода чтения/разбора лога обратно из
 *             памяти.
 *
 *          === ЧЕСТНЫЕ ОГРАНИЧЕНИЯ ===
 *
 *          - Вывод по умолчанию (SWO/ITM_SendChar) работает только там, где
 *            физически есть блок ITM:
 *              РАБОТАЕТ    - STM32F1/F2/L1 (Cortex-M3); STM32F3/F4/G4/L4/L4+
 *                            (Cortex-M4); STM32F7/H7 (Cortex-M7); STM32L5/
 *                            U5/H5/WBA (Cortex-M33); STM32WB/WL - только
 *                            основное ядро CPU1 (Cortex-M4).
 *              НЕ РАБОТАЕТ - STM32F0 (Cortex-M0); STM32G0/L0/C0 (Cortex-M0+);
 *                            STM32WB/WL - радио-сопроцессор CPU2 (Cortex-M0+).
 *            На МК/ядре без ITM обязательно передавайте свою console_fn при
 *            инициализации, иначе сборка не пройдёт (ITM_SendChar не
 *            объявлена в CMSIS для этих ядер).
 *          - Библиотека не потокобезопасна/не реентерабельна сама по себе
 *            (никаких критических секций внутри) - если LOGGER_Log()/
 *            LOGGER_Flush() могут быть вызваны одновременно из основного кода
 *            и из прерывания (или из нескольких прерываний разного
 *            приоритета), оборачивайте вызовы в критическую секцию на
 *            стороне пользователя самостоятельно.
 *          - Метки времени записи и подсчёт частоты вызова конкретного кода
 *            лога в этой версии не реализованы (см. Список_идей проекта) -
 *            осознанно отложено на будущее.
 ******************************************************************************
 */

#ifndef LOGGER_H
#define LOGGER_H

#ifdef __cplusplus
extern "C" {
#endif

#include <stdint.h>
#include "main.h"   /* CubeMX: HAL_StatusTypeDef */

/* ------------------------------------------------------------------------ */
/*  Конфигурация модуля (define-ы, меняются до включения заголовка)         */
/* ------------------------------------------------------------------------ */

/** Ёмкость общего статического буфера записей (без malloc). Верхний предел -
 *  256, жёстко ограничен библиотекой: пороги приоритетов задаются как
 *  uint8_t (0..255), чтобы пользователь конфигурацией не мог случайно выйти
 *  за границы буфера. Можно уменьшить (например, для МК с малым ОЗУ) через
 *  #define LOGGER_BUFFER_CAPACITY перед включением этого заголовка, но не
 *  увеличить сверх 256. */
#ifndef LOGGER_BUFFER_CAPACITY
#define LOGGER_BUFFER_CAPACITY       256U
#endif
#if LOGGER_BUFFER_CAPACITY > 256U
#error "LOGGER_BUFFER_CAPACITY не может превышать 256 (пороги приоритетов - uint8_t)"
#endif

/** Жёсткий предел длины текстового описания записи таблицы кодов (символов,
 *  без учёта завершающего '\0'). Рекомендуемая длина - 32-64 символа (см.
 *  logger_codes.h), проверяется в LOGGER_Init(). */
#define LOGGER_MAX_DESCRIPTION_LENGTH 64U

/* ------------------------------------------------------------------------ */
/*  Приоритет лога - 3 уровня                                               */
/* ------------------------------------------------------------------------ */

typedef enum
{
    LOGGER_PRIORITY_LOW    = 0,  /**< низкий приоритет - как правило, реже пишется в память */
    LOGGER_PRIORITY_MEDIUM = 1,  /**< средний приоритет */
    LOGGER_PRIORITY_HIGH   = 2   /**< высокий приоритет - как правило, чаще пишется в память */
} LOGGER_Priority_t;

/** Количество уровней приоритета - используется для размера внутренних
 *  массивов (порогов). Значения LOGGER_Priority_t всегда 0..LOGGER_PRIORITY_COUNT-1. */
#define LOGGER_PRIORITY_COUNT 3U

/* ------------------------------------------------------------------------ */
/*  Запись таблицы кодов логов - см. logger_codes.h                        */
/* ------------------------------------------------------------------------ */

typedef struct
{
    /** Уникальный 32-битный код лога, указывается в HEX (см. рекомендацию по
     *  нумерации в шапке logger_codes.h). Таблица должна быть отсортирована
     *  по этому полю по возрастанию, без повторов. */
    uint32_t code;

    /** Приоритет данного кода лога - один из 3 уровней. */
    LOGGER_Priority_t priority;

    /** Короткое текстовое описание - только для вывода в консоль/SWO, в
     *  энергонезависимую память не попадает. Рекомендуемая длина - 32-64
     *  символа, жёсткий предел - LOGGER_MAX_DESCRIPTION_LENGTH. */
    const char *description;
} LOGGER_LogEntry_t;

/* ------------------------------------------------------------------------ */
/*  Формат одной записи в буфере / в данных, переданных в write_fn          */
/* ------------------------------------------------------------------------ */

typedef struct
{
    uint32_t code;   /**< код лога (как в LOGGER_LogTable) */
    int32_t  value;  /**< значение переменной, переданное в LOGGER_Log() */
} LOGGER_Record_t;

/* ------------------------------------------------------------------------ */
/*  Пользовательские функции вывода/сохранения                              */
/* ------------------------------------------------------------------------ */

/**
 * @brief  Функция сохранения накопленных логов в энергонезависимую память,
 *         предоставляется пользователем при инициализации. Вызывается, когда
 *         по текущему максимальному приоритету буфера достигнут настроенный
 *         порог. Должна быть быстрой и не блокирующей надолго (вызывается из
 *         того же контекста, что и LOGGER_Log()).
 * @param  context      значение write_context, переданное в LOGGER_Init()
 * @param  data         указатель на массив LOGGER_Record_t подряд (буфер
 *                       логгера) - только на время вызова, копировать данные
 *                       нужно самостоятельно, если требуется асинхронная запись
 * @param  length_bytes  длина data в байтах (кратна sizeof(LOGGER_Record_t))
 */
typedef void (*LOGGER_MemoryWriteFn_t)(void *context, const uint8_t *data, uint32_t length_bytes);

/**
 * @brief  Функция вывода одной записи лога в консоль, предоставляется
 *         пользователем при инициализации - заменяет вывод по умолчанию в
 *         SWO. Вызывается на каждый LOGGER_Log(), должна быть быстрой.
 * @param  context      значение console_context, переданное в LOGGER_Init()
 * @param  code         код лога
 * @param  priority     приоритет лога (из таблицы кодов)
 * @param  value        значение переменной, переданное в LOGGER_Log()
 * @param  description  текстовое описание из таблицы кодов (NULL, если код
 *                       не найден в таблице)
 */
typedef void (*LOGGER_ConsoleWriteFn_t)(void *context, uint32_t code, LOGGER_Priority_t priority,
                                         int32_t value, const char *description);

/* ------------------------------------------------------------------------ */
/*  Конфигурация инициализации                                              */
/* ------------------------------------------------------------------------ */

typedef struct
{
    /** Функция записи буфера логов в энергонезависимую память. NULL - логи
     *  не буферизируются и не сохраняются, только выводятся. */
    LOGGER_MemoryWriteFn_t write_fn;

    /** Указатель, который будет передан write_fn первым аргументом (например,
     *  хэндл/заголовок драйвера памяти пользователя). Может быть NULL. */
    void *write_context;

    /** Функция вывода лога в консоль. NULL - используется вывод по умолчанию
     *  через SWO (ITM). */
    LOGGER_ConsoleWriteFn_t console_fn;

    /** Указатель, который будет передан console_fn первым аргументом. Может
     *  быть NULL. */
    void *console_context;

    /** Сколько записей НИЗКОГО приоритета должно накопиться в буфере (в
     *  качестве текущего максимального приоритета буфера), чтобы вызвать
     *  запись в память. 1..255. Учитывается, только если write_fn != NULL. */
    uint8_t threshold_low;

    /** Аналогично для СРЕДНЕГО приоритета. Рекомендуется <= threshold_low. */
    uint8_t threshold_medium;

    /** Аналогично для ВЫСОКОГО приоритета. Рекомендуется <= threshold_medium -
     *  логи высокого приоритета обычно должны попадать в память чаще. */
    uint8_t threshold_high;
} LOGGER_Config_t;

/* ------------------------------------------------------------------------ */
/*  Инициализация                                                           */
/* ------------------------------------------------------------------------ */

/**
 * @brief  Инициализирует логгер: проверяет корректность таблицы кодов
 *         (logger_codes.h - отсортирована по возрастанию, без повторов,
 *         приоритеты и длины описаний корректны) и конфигурации, применяет
 *         колбэки и пороги буферизации. Вся валидация выполняется здесь, один
 *         раз, чтобы LOGGER_Log() не делал проверок в рантайме.
 *
 *         Необязательна - без вызова LOGGER_Init() (или до него) LOGGER_Log()
 *         работает в минимальном режиме "только вывод в SWO".
 *
 *         Повторный вызов (реинициализация) сначала сбрасывает в память уже
 *         накопленный буфер (если ранее была включена буферизация и в нём
 *         есть записи), затем применяет новую конфигурацию.
 *
 * @param  config  заполненная конфигурация
 * @retval HAL_OK; HAL_ERROR - config == NULL, таблица кодов пуста/не
 *         отсортирована/содержит повтор кода/некорректный приоритет/слишком
 *         длинное описание, либо (при заданном write_fn) один из порогов
 *         равен 0 или превышает LOGGER_BUFFER_CAPACITY
 */
HAL_StatusTypeDef LOGGER_Init(const LOGGER_Config_t *config);

/* ------------------------------------------------------------------------ */
/*  Приём лога - главная горячая функция                                    */
/* ------------------------------------------------------------------------ */

/**
 * @brief  Логирует событие: выводит его в консоль/SWO и, если включена
 *         буферизация, добавляет в буфер (см. архитектуру в шапке файла).
 *         Крайне часто вызываемая функция - оптимизирована по скорости,
 *         никаких проверок конфигурации в рантайме.
 *
 *         Если code отсутствует в таблице кодов - запись всё равно выводится
 *         в консоль/SWO (с описанием "???", приоритет трактуется как
 *         LOGGER_PRIORITY_HIGH только для целей вывода), но НЕ буферизуется
 *         (приоритет для неё достоверно не определён) - это сигнал
 *         разработчику о рассинхронизации кода лога с таблицей.
 *
 * @param  code   код лога (см. таблицу в logger_codes.h)
 * @param  value  значение переменной, сопровождающее событие
 */
void LOGGER_Log(uint32_t code, int32_t value);

/* ------------------------------------------------------------------------ */
/*  Принудительный сброс буфера                                             */
/* ------------------------------------------------------------------------ */

/**
 * @brief  Немедленно записывает накопленный буфер в память через write_fn
 *         (если буферизация включена и в буфере есть записи), не дожидаясь
 *         достижения порога. Полезно перед переходом в сон/перед обновлением
 *         прошивки, чтобы не потерять ещё не сброшенные логи.
 * @retval HAL_OK - буфер пуст либо успешно сброшен; HAL_ERROR - буферизация
 *         не включена (LOGGER_Init() не вызывался или write_fn == NULL)
 */
HAL_StatusTypeDef LOGGER_Flush(void);

#ifdef __cplusplus
}
#endif

#endif /* LOGGER_H */
